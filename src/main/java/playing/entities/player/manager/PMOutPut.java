package playing.entities.player.manager;

import playing.entities.player.PlayerManager;

public class PMOutPut extends PMModule{

    public PMOutPut(PlayerManager playerManager) {
        super(playerManager);
    }
}
